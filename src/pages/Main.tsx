import React from "react";
import Intro from "../components/intro/Intro";

const MainPage = () => {
  return (
    <React.Fragment>
      {/* <p onClick={testSend}>main</p> */}
      <Intro />
    </React.Fragment>
  );
};

export default MainPage;
